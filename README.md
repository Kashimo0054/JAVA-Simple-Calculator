# JAVA-Simple-Calculator
Calculator

A a simple program that prompts the user to enter an operation (e.g., "+" for addition, "-" for subtraction, etc.). Then, ask the user to input two numbers. Use a third variable to store the result of the calculation within an if-else block and print the final output.







